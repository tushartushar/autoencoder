public String toString(){
  String result="(";
  for (int i=0; i < _coordinate.length - 1; i++)   result+=(_coordinate[i] + ",");
  result+=_coordinate[_coordinate.length - 1] + ")";
  return result;
}

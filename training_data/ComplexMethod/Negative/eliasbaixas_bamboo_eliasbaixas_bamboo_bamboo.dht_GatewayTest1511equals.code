public boolean equals(Object rhs){
  PutInfo other=(PutInfo)rhs;
  if (other.key.length != key.length)   return false;
  for (int i=0; i < key.length; ++i)   if (key[i] != other.key[i])   return false;
  return true;
}

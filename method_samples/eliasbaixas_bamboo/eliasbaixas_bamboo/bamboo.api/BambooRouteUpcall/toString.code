public String toString(){
  return "(BambooRouteUpcall src=" + GuidTools.guid_to_string(src) + " dest="+ GuidTools.guid_to_string(dest)+ " immediate_src="+ immediate_src+ " app_id="+ Long.toHexString(app_id)+ " iter="+ iter+ " tries="+ tries+ " wait_ms="+ wait_ms+ " est_rtt_ms="+ est_rtt_ms+ " payload="+ " payload="+ payload+ ")";
}

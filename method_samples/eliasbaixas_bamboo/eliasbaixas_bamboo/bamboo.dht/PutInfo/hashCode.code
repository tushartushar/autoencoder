public int hashCode(){
  ByteBuffer bb=ByteBuffer.wrap(key);
  return bb.getInt();
}
public int hashCode(){
  int result=0;
  for (int i=0; i < key.length; ++i)   result=result * 7 + (((int)key[i]) & 0xff);
  return result;
}
public int hashCode(){
  ByteBuffer bb=ByteBuffer.wrap(key);
  return bb.getInt();
}

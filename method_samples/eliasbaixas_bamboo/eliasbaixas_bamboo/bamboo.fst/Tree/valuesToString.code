public String valuesToString(Node node,long aoff){
  String result="";
  PointList[] pl=valuesToPoints(node,aoff);
  for (PointList i=pl[0]; i != null; i=i.next)   result+="(" + i.x + ", "+ i.y+ ") ";
  return result;
}
